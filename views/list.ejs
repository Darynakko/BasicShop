<!doctype html>
<html>
  <head>
    <style>
        body {background-color: blanchedalmond;}
        title, h1, p {font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;}
    </style>
    <title class="title"><%=shopName%></title>
  </head>
  <body>
    <h1><%=shopName%>'s shops addresses</h1>
    <p>We have shops all over the world</p>
    <ul><% for(let i = 0; i < shopAddresses.length; i++) { %>
        <li><%= shopAddresses[i] %></li>
      <% } %></ul>
  </body>
</html>
